name = twitter-source-connector
tasks.max = 1
connector.class = com.github.jcustenborder.kafka.connect.twitter.TwitterSourceConnector

# Set these required values
process.deletes = false
filter.keywords = bitcoin
kafka.status.topic = twitter-status-topic
kafka.delete.topic = twitter-delete-topic

twitter.oauth.consumerKey = <key>
twitter.oauth.consumerSecret = <secret>
twitter.oauth.accessToken = <key>
twitter.oauth.accessTokenSecret = <secret>